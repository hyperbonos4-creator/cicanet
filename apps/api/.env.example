# API CICANET — variables de entorno (todas tienen default para la demo)
API_PORT=4000
CORS_ORIGINS=http://localhost:3000,http://localhost:3100

# Secretos JWT — CAMBIAR en producción por valores largos y aleatorios
JWT_ACCESS_SECRET=cicanet-dev-access-secret-change-me
JWT_REFRESH_SECRET=cicanet-dev-refresh-secret-change-me
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Admin semilla
SEED_ADMIN_USER=admin
SEED_ADMIN_PASS=cicanet2026

# Geocodificación: 'mapbox' (preciso, requiere token gratuito) o 'nominatim' (OSM, sin clave)
GEOCODER_PROVIDER=mapbox
MAPBOX_TOKEN=
